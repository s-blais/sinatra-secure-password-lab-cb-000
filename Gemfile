# A sample Gemfile
source 'https://rubygems.org'

gem 'sinatra'
gem 'activerecord', '<=5.1', :require => 'active_record'
gem 'sinatra-activerecord'
gem 'rake'
gem 'bcrypt'
gem 'require_all'
gem 'capybara'
gem 'rack_session_access'
gem 'rack-test'
gem 'shotgun'
gem 'pry'
gem 'json', '~>1.7' # I had to add this to resolve conflicts before I could do anything. Not sure why. I keep having to mess with the Gemfiles on the last few lessons and UGH. Yet, the Gemfile.lock shows 1.8.6; I don't understaaaaaand

group :development do
  gem 'sqlite3', '~> 1.3.6' #I had to add this version control too? Whhhyyyyyyy
  gem 'rspec'
  gem 'tux'
end
